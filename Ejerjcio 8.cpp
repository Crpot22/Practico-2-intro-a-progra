#include <iostream>
#include <string>
using namespace std;
int main() {
    string a, b, c;
    cin >> a >> b >> c;

    if (b < a) {
        a = b;
    }
    if (c < menor) {
        a= c;
    }

    cout << a << endl;

    return 0;
}
